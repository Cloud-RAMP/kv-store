# KV-Store

Placeholder README. update this guy later

To run, run the command `go run cmd/kv-store/main.go` (probably can improve this later)

To test, make sure the server is running and then run `python3 test/benchmark.py`
